// Auto-generated by rads-mask v0.0.5 "CHAMELEON"
// Transpiled from Python to RADS (manually cleaned)

blast add(a, b) {
    return a + b;
}

blast multiply(a, b) {
    return a * b;
}

blast main() {
    turbo x = 10;
    turbo y = 5;

    turbo sum_result = add(x, y);
    turbo product_result = multiply(x, y);

    echo("Sum: " + sum_result);
    echo("Product: " + product_result);
}
