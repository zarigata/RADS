/*
 * stdlib_glob.h — RADS glob module
 *
 * Unix-style pathname pattern expansion.
 * Equivalent to Python's glob module.
 */

#ifndef STDLIB_GLOB_H
#define STDLIB_GLOB_H

#include "../core/interpreter.h"

void stdlib_glob_register(void);

#endif
