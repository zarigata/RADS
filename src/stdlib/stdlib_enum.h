/*
 * stdlib_enum.h — RADS enum module
 *
 * Enumeration type support.
 * Equivalent to Python's enum module (simplified).
 */

#ifndef STDLIB_ENUM_H
#define STDLIB_ENUM_H

#include "../core/interpreter.h"

void stdlib_enum_register(void);

#endif
